Step 1: logon to MySQL with the admin ID and run the script MySQLData/create_mysql_script_security
This will setup Security DB, create the needed tables and populate them with the test data

Step 2: run AuthenticationApp from NetBeans (same as other apps in A2)